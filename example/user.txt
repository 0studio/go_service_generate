package user

import (
	"time"
)

type User struct {
	id   int    `mysql:"pk,defalut=1,type=bigint"` // id
	name string `mysql:"pk,defalut='hello',name=helloName,type=varchar(10)"`
	age  int
	sex  bool
	t    time.Time `mysql:"type=int"`
	t2   time.Time
}
